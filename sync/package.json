{
  "name": "le-parfum-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sincronização do catálogo Le Parfum com o Bling (API v3) + servidor local do catálogo",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "auth": "node src/auth.js",
    "sync": "node src/sync.js",
    "quick": "node src/quick.js",
    "images": "node src/images.js",
    "bundle": "node src/bundle.js",
    "curar": "node src/curate.js",
    "atualizar": "npm run sync && npm run images && npm run bundle",
    "server": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "sharp": "^0.35.3",
    "ws": "^8.21.1"
  }
}
